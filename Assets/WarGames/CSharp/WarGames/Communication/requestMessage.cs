using UnityEngine;
using System.Collections;

namespace WarGames.Communication {
    /// <summary>
    /// Message sent by leaders when they want the reciving agent to send a informationMessage.
    /// Used to check the reciving agents progress on it's goal, or check if an agent is still alive.
    /// </summary>
    /// <seealso cref="WarGames.Communication.informationMessage" />
    public class requestMessage : MonoBehaviour {

        // Use this for initialization
        void Start() {

        }

        // Update is called once per frame
        void Update() {

        }
    }
}